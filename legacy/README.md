# Legacy source

`legacy_dynet_wrapper.hpp` is the uploaded C++ DyNet wrapper snapshot used as architectural reference.

Do not treat it as code to compile in this Rust workspace. Its concepts are mapped in `../docs/legacy-mapping.md`.
